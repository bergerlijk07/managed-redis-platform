server:
  port: 8080

spring:
  application:
    name: managed-redis-platform
  datasource:
    url: jdbc:h2:mem:redisplatform;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

# Platform Configuration
platform:
  reconciler:
    interval-seconds: 30
  cloud:
    provider: aws
    region: us-east-1
    account-id: "${AWS_ACCOUNT_ID:123456789012}"
    vpc-cidr: "${AWS_VPC_CIDR:10.100.0.0/16}"
    route53-hosted-zone-id: "${AWS_ROUTE53_ZONE_ID:}"
    dns-suffix: "${AWS_DNS_SUFFIX:redis.platform.internal}"
  kubernetes:
    master-url: "${K8S_MASTER_URL:}"
    namespace: "${K8S_NAMESPACE:default}"
    kubeconfig: "${KUBECONFIG_PATH:}"

# Observability
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: managed-redis-platform
  tracing:
    sampling:
      probability: 1.0

logging:
  level:
    io.platform.redis: INFO
    org.springframework: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId}] [%X{operationId}] %-5level %logger{36} - %msg%n"

---
# Production profile
spring:
  config:
    activate:
      on-profile: production
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:managed_redis}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
